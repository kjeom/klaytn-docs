# Account Update
# Basic
<CH.Spotlight>
<CH.Code>

```js TxTypeAccountUpdate.js
const ethers = require("ethers");
import { crypto } from "@ethersproject/crypto";
const { Wallet } = require("@klaytn/ethers-ext");

const senderAddr = '0xe15cd70a41dfb05e7214004d7d054801b2a2f06b' 
const senderPriv = '0xc9668ccd35fc20587aa37a48838b48ccc13cf14dd74c8999dd6a480212d5f7ac' 
const senderNewPriv = '0x0e4ca6d38096ad99324de0dde108587e5d7c600165ae4cd6c2462c597458c2b8' 

async function main() {
  const provider = new ethers.providers.JsonRpcProvider('https://public-en-baobab.klaytn.net')
  const wallet = new Wallet(senderPriv, provider);

  let tx = {
        type: 0x20,
        from: senderAddr,
        key: {
            type: 0x02, 
            key: crypto.computePublicKey(senderNewPriv, true),
        }
    };
  
  const sentTx = await wallet.sendTransaction(tx);
  console.log('sentTx', sentTx);

  const rc = await sentTx.wait();
  console.log('receipt', rc);
}

main();

```




---
```zsh output
❯ node functionName.js
output
```
</CH.Code>






---

Import tandard Ethereum ethers library.

```js TxTypeAccountUpdate.js focus=1

```
---

Importing the crypto module from ethersproject

```js TxTypeAccountUpdate.js focus=2

```
---

Klaytn-specific extensions for wallet utils.

```js TxTypeAccountUpdate.js focus=3

```
---

Declaring sender address and private keys

```js TxTypeAccountUpdate.js focus=5:7

```
---
Creating a JSON-RPC provider instance with the specified URL


```js TxTypeAccountUpdate.js focus=10

```
---

Also, you can change the default provider. For example, using the alchemy provider.

```js txTypeLegacyTransaction.js focus=10
const ethers = require("ethers");
import { crypto } from "@ethersproject/crypto";
const { Wallet } = require("@klaytn/ethers-ext");

const senderAddr = '0xe15cd70a41dfb05e7214004d7d054801b2a2f06b' 
const senderPriv = '0xc9668ccd35fc20587aa37a48838b48ccc13cf14dd74c8999dd6a480212d5f7ac' 
const senderNewPriv = '0x0e4ca6d38096ad99324de0dde108587e5d7c600165ae4cd6c2462c597458c2b8' 

async function main() {
  const provider = new ethers.providers.JsonRpcProvider('https://klaytn-mainnet-rpc.allthatnode.com:8551')
  const wallet = new Wallet(senderPriv, provider);

  let tx = {
        type: 0x20,
        from: senderAddr,
        key: {
            type: 0x02, 
            key: crypto.computePublicKey(senderNewPriv, true),
        }
    };
  
  const sentTx = await wallet.sendTransaction(tx);
  console.log('sentTx', sentTx);

  const rc = await sentTx.wait();
  console.log('receipt', rc);
}

main();
    
```
---

Creating a transaction object 

```js TxTypeAccountUpdate.js focus=13:20

```
---
Computing the public key from the new private key


```js TxTypeAccountUpdate.js focus=18

```
---

Sending the transaction

```js TxTypeAccountUpdate.js focus=22:23

```
---

Waiting for the transaction receipt

```js TxTypeAccountUpdate.js focus=25:26

```


</CH.Spotlight>


# Fee Delegation


<CH.Spotlight>
<CH.Code>

```js TxTypeFeeDelegatedAccountUpdate.js
const { Wallet, TxType, AccountKeyType } = require("@klaytn/ethers-ext");
const ethers = require("ethers");

// create new account for testing in https://baobab.wallet.klaytn.foundation/
const senderAddr = "0x0adc9d67eef6d0f02e17543386be40ed451f7667";
const senderPriv = "0xf8cc7c3813ad23817466b1802ee805ee417001fcce9376ab8728c92dd8ea0a6b";
const senderNewPriv = "0xf8cc7c3813ad23817466b1802ee805ee417001fcce9376ab8728c92dd8ea0a6b";
const feePayerAddr = "0xcb0eb737dfda52756495a5e08a9b37aab3b271da";
const feePayerPriv = "0x9435261ed483b6efa3886d6ad9f64c12078a0e28d8d80715c773e16fc000cff4";

async function main() {
  const provider = new ethers.providers.JsonRpcProvider("https://public-en-baobab.klaytn.net");
  const senderWallet = new Wallet(senderAddr, senderPriv, provider);
  const feePayerWallet = new Wallet(feePayerPriv, provider);

  let tx = {
    type: TxType.FeeDelegatedAccountUpdateWithRatio,
    from: senderAddr,
    key: {
      type: AccountKeyType.Public,
      key: ethers.utils.computePublicKey(senderNewPriv, true)
    },
    feeRatio: 40,
    gasLimit: 60000,
  };

  tx = await senderWallet.populateTransaction(tx);
  console.log(tx);

  const senderTxHashRLP = await senderWallet.signTransaction(tx);
  console.log("senderTxHashRLP", senderTxHashRLP);

  const sentTx = await feePayerWallet.sendTransactionAsFeePayer(senderTxHashRLP);
  console.log("sentTx", sentTx);

  const rc = await sentTx.wait();
  console.log("receipt", rc);
}

main();

```




---
```zsh output
❯ node TxTypeFeeDelegatedAccountUpdate.js
output
```
</CH.Code>



---

Import tandard Ethereum ethers library.

```js TxTypeFeeDelegatedAccountUpdate.js focus=1

```
---

Importing the crypto module from ethersproject

```js TxTypeFeeDelegatedAccountUpdate.js focus=2

```
---

Klaytn-specific extensions for wallet utils.

```js TxTypeFeeDelegatedAccountUpdate.js focus=3

```
---

Declaring sender address and private keys

```js TxTypeFeeDelegatedAccountUpdate.js focus=5:7

```
---
Creating a JSON-RPC provider instance with the specified URL


```js TxTypeFeeDelegatedAccountUpdate.js focus=10

```
---

Also, you can change the default provider. For example, using the alchemy provider.

```js TxTypeFeeDelegatedAccountUpdate.js focus=10
const { Wallet, TxType, AccountKeyType } = require("@klaytn/ethers-ext");
const ethers = require("ethers");

// create new account for testing in https://baobab.wallet.klaytn.foundation/
const senderAddr = "0x0adc9d67eef6d0f02e17543386be40ed451f7667";
const senderPriv = "0xf8cc7c3813ad23817466b1802ee805ee417001fcce9376ab8728c92dd8ea0a6b";
const senderNewPriv = "0xf8cc7c3813ad23817466b1802ee805ee417001fcce9376ab8728c92dd8ea0a6b";
const feePayerAddr = "0xcb0eb737dfda52756495a5e08a9b37aab3b271da";
const feePayerPriv = "0x9435261ed483b6efa3886d6ad9f64c12078a0e28d8d80715c773e16fc000cff4";

async function main() {
  const provider = new ethers.providers.JsonRpcProvider("https://public-en-baobab.klaytn.net");
  const senderWallet = new Wallet(senderAddr, senderPriv, provider);
  const feePayerWallet = new Wallet(feePayerPriv, provider);

  let tx = {
    type: TxType.FeeDelegatedAccountUpdateWithRatio,
    from: senderAddr,
    key: {
      type: AccountKeyType.Public,
      key: ethers.utils.computePublicKey(senderNewPriv, true)
    },
    feeRatio: 40,
    gasLimit: 60000,
  };

  tx = await senderWallet.populateTransaction(tx);
  console.log(tx);

  const senderTxHashRLP = await senderWallet.signTransaction(tx);
  console.log("senderTxHashRLP", senderTxHashRLP);

  const sentTx = await feePayerWallet.sendTransactionAsFeePayer(senderTxHashRLP);
  console.log("sentTx", sentTx);

  const rc = await sentTx.wait();
  console.log("receipt", rc);
}

main();
```
---

Creating a transaction object 

```js TxTypeFeeDelegatedAccountUpdate.js focus=13:20

```
---
Computing the public key from the new private key


```js TxTypeFeeDelegatedAccountUpdate.js focus=18

```
---

Sending the transaction

```js TxTypeFeeDelegatedAccountUpdate.js focus=22:23

```
---

Waiting for the transaction receipt

```js TxTypeFeeDelegatedAccountUpdate.js focus=25:26

```


</CH.Spotlight>