# Public Account Key

**AccountKeyPublic** is used for accounts having one public key. If an account has an AccountKeyPublic object, the transaction validation process is done like below:

<CH.Spotlight>
<CH.Code>

```js recoverPublicKey.js
const { ethers } = require("ethers");
const { Wallet, TxType, AccountKeyType, parseKlay } = require("@klaytn/ethers-ext");

const senderAddr = "0xe15cd70a41dfb05e7214004d7d054801b2a2f06b";
const senderPriv = "0x0e4ca6d38096ad99324de0dde108587e5d7c600165ae4cd6c2462c597458c2b8";
const senderNewPriv = "0x0e4ca6d38096ad99324de0dde108587e5d7c600165ae4cd6c2462c597458c2b8";
const recieverAddr = "0xc40b6909eb7085590e1c26cb3becc25368e249e9";

const provider = new ethers.providers.JsonRpcProvider("https://public-en-baobab.klaytn.net");
const wallet = new Wallet(senderAddr, senderPriv, provider);
const newWallet = new Wallet(senderAddr, senderNewPriv, provider);

async function updateAccount() {
  let senderNewPub = ethers.utils.computePublicKey(senderNewPriv, true);

  const tx = {
    type: TxType.AccountUpdate,
    from: senderAddr,
    key: {
      type: AccountKeyType.Public,
      key: senderNewPub,
    }
  };

  const sentTx = await wallet.sendTransaction(tx);
  console.log("sentTx", sentTx);

  const receipt = await sentTx.wait();
  console.log("receipt", receipt);
}

async function sendTx() {
  let tx = {
    type: TxType.ValueTransfer,
    from: senderAddr,
    to: recieverAddr,
    value: parseKlay("0.01"),
  };

  const sentTx = await newWallet.sendTransaction(tx);
  console.log("sentTx", sentTx);

  const receipt = await sentTx.wait();
  console.log("receipt", receipt);
}

async function recoverMsg() {
  const msg = "hello";
  const msghex = ethers.utils.hexlify(ethers.utils.toUtf8Bytes(msg));
  const sig = await newWallet.signMessage(msg);
  console.log({ senderAddr, msg, msghex, sig });

  const addr1 = ethers.utils.verifyMessage(msg, sig);
  console.log("recoveredAddr lib", addr1, addr1.toLowerCase() === newWallet.address.toLowerCase());

  const addr2 = await provider.send("klay_recoverFromMessage", [senderAddr, msghex, sig, "latest"]);
  console.log("recoveredAddr rpc", addr2, addr2.toLowerCase() === newWallet.address.toLowerCase());
}

async function main() {
  await updateAccount();
  await sendTx();
  await recoverMsg();
}
main().catch(console.error);

```




---
```zsh output
❯ node recoverPublicKey.js
output
```
</CH.Code>

---
Import necessary utils from ethers and @klaytn/ether-ext
```js recoverPublicKey.js focus=1:2

```
---
Define sender address and private keys, receiver address
```js recoverPublicKey.js focus=4:7

```
---
Create a web3 connectoin using JsonRpcProvider and baobab RPC endpoint
```js recoverPublicKey.js focus=9

```
---
Create 2 different wallets using defined private keys
```js recoverPublicKey.js focus=10:11

```
---
Update the account
```js recoverPublicKey.js focus=61

```
---
create new public key from **senderNewPriv** key
```js recoverPublicKey.js focus=14

```
---
create a transaction object to update sender account with new private key
```js recoverPublicKey.js focus=16:23

```
---

Use the wallet to send the transaction to network
```js recoverPublicKey.js focus=25:26

```
---
Wait for the transaction receipt
```js recoverPublicKey.js focus=28:29

```
---
Send a transfer transaction
```js recoverPublicKey.js focus=62

```
---
Create a transfer transaction object
```js recoverPublicKey.js focus=33:38

```
---
Use the wallet to send the transaction to network
```js recoverPublicKey.js focus=40:41

```
---
Wait for the transaction receipt
```js recoverPublicKey.js focus=43:44

```
---
Recover the address from message
```js recoverPublicKey.js focus=63

```
---
sign a message with the new wallet
```js recoverPublicKey.js focus=48:51

```
---
Retreive wallet address from the signed message
```js recoverPublicKey.js focus=53:54

```
---
Recover the address from message use klay_recoverFromMessage
```js recoverPublicKey.js focus=56:57

```



</CH.Spotlight>
