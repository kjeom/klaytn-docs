# Value Transfer Memo
**TxTypeValueTransferMemo** is used when a user wants to send KLAY with a specific message.
# Basic
<CH.Spotlight>
<CH.Code>

```java ValueTransferMemo.java
package org.web3j.example;

import java.io.IOException;
import java.math.BigInteger;
import org.web3j.crypto.KlayCredentials;
import org.web3j.crypto.KlayRawTransaction;
import org.web3j.crypto.KlayTransactionEncoder;
import org.web3j.crypto.transaction.type.TxType;
import org.web3j.crypto.transaction.type.TxTypeValueTransfer;
import org.web3j.crypto.transaction.type.TxType.Type;
import org.web3j.protocol.core.DefaultBlockParameterName;
import org.web3j.protocol.core.methods.response.EthChainId;
import org.web3j.protocol.core.methods.response.EthSendTransaction;
import org.web3j.protocol.http.HttpService;
import org.web3j.protocol.klaytn.Web3j;
import org.web3j.utils.Numeric;
import org.web3j.protocol.klaytn.core.method.response.TransactionReceipt;

/**
 * 
 */
public class ValueTransferMemoExample implements keySample {
    /**
     * 
     */

    public static void run() throws IOException {

        Web3j web3j = Web3j.build(new HttpService(keySample.BAOBAB_URL));
        KlayCredentials credentials = KlayCredentials.create(keySample.LEGACY_KEY_privkey);

        BigInteger GAS_PRICE = BigInteger.valueOf(50000000000L);
        BigInteger GAS_LIMIT = BigInteger.valueOf(6721950);
        String from = credentials.getAddress();
        EthChainId EthchainId = web3j.ethChainId().send();
        long chainId = EthchainId.getChainId().longValue();
        String to = "0x000000000000000000000000000000000000dead";
        BigInteger nonce = web3j.ethGetTransactionCount(from, DefaultBlockParameterName.LATEST).send()
                .getTransactionCount();
        BigInteger value = BigInteger.valueOf(100);

        String data = "Klaytn Web3j";
        byte[] payload = data.getBytes();

        TxType.Type type = Type.VALUE_TRANSFER_MEMO;

        KlayRawTransaction raw = KlayRawTransaction.createTransaction(
                type,
                nonce,
                GAS_PRICE,
                GAS_LIMIT,
                to,
                value,
                from,
                payload);

        byte[] signedMessage = KlayTransactionEncoder.signMessage(raw, chainId, credentials);
        String hexValue = Numeric.toHexString(signedMessage);
        EthSendTransaction transactionResponse = web3j.ethSendRawTransaction(hexValue).send();
        System.out.println("TxHash : \n " + transactionResponse.getResult());
        String txHash = transactionResponse.getResult();
        try {
            Thread.sleep(2000);
        } catch (Exception e) {
            System.out.println(e);
        }
        TransactionReceipt receipt = web3j.klayGetTransactionReceipt(txHash).send().getResult();
        System.out.println("receipt : \n" + receipt);
        web3j.shutdown();

        TxTypeValueTransfer rawTransaction = TxTypeValueTransfer.decodeFromRawTransaction(signedMessage);
        System.out.println("TxType : " + rawTransaction.getKlayType());
    }

}

```




---
```zsh output
❯ node functionName.js
output
```
</CH.Code>






---

Import Web3j and related classes for blockchain interaction.
```java ValueTransferMemo.java focus=1:17

```

---
Create a Web3j instance connected to the BAOBAB testnet.
```java ValueTransferMemo.java focus=29

```

---
Load Klaytn credentials from a private key.
```java ValueTransferMemo.java focus=30

```

---
Define gas price and gas limit for the transaction.
```java ValueTransferMemo.java focus=32:33

```

---
Declare sender's address.
```java ValueTransferMemo.java focus=34

```

---
Get the chain ID from the connected Klaytn node.
```java ValueTransferMemo.java focus=35:36

```

---
Other parameters like recipent, nonce, value, data
```java ValueTransferMemo.java focus=37:45

```
---
Create a raw transaction object.
```java ValueTransferMemo.java focus=47

```
---
Sign the raw transaction with the sender's private key.
```java ValueTransferMemo.java focus=57:58

```
---
Send the signed transaction to the Klaytn node.
```java ValueTransferMemo.java focus=59:60

```
---
Retrieve and print the transaction receipt.
```java ValueTransferMemo.java focus=61:68

```
---
Shutdown the Web3j instance.
```java ValueTransferMemo.java focus=69

```
---
Decode the raw transaction to get the transaction type.
```java ValueTransferMemo.java focus=71:72

```

</CH.Spotlight>

# Fee Delegation


<CH.Spotlight>
<CH.Code>

```java FeeDelegatedValueTransferMemo.java

package org.web3j.example;

import java.io.IOException;
import java.math.BigInteger;
import org.web3j.crypto.KlayCredentials;
import org.web3j.crypto.KlayRawTransaction;
import org.web3j.crypto.KlayTransactionEncoder;
import org.web3j.crypto.transaction.type.TxType;
import org.web3j.crypto.transaction.type.TxTypeValueTransfer;
import org.web3j.crypto.transaction.type.TxType.Type;
import org.web3j.protocol.core.DefaultBlockParameterName;
import org.web3j.protocol.core.methods.response.EthChainId;
import org.web3j.protocol.core.methods.response.EthSendTransaction;
import org.web3j.protocol.http.HttpService;
import org.web3j.protocol.klaytn.Web3j;
import org.web3j.utils.Numeric;
import org.web3j.protocol.klaytn.core.method.response.TransactionReceipt;

public class FeeDelegatedValueTransferMemoExample {

	public static void run() throws IOException {
		Web3j web3j = Web3j.build(new HttpService(keySample.BAOBAB_URL));
		KlayCredentials credentials = KlayCredentials.create(keySample.LEGACY_KEY_privkey);
		KlayCredentials credentials_feepayer = KlayCredentials.create(keySample.LEGACY_KEY_FEEPAYER_privkey);

		BigInteger GAS_PRICE = BigInteger.valueOf(50000000000L);
		BigInteger GAS_LIMIT = BigInteger.valueOf(6721950);
		String from = credentials.getAddress();
		EthChainId EthchainId = web3j.ethChainId().send();
		long chainId = EthchainId.getChainId().longValue();
		String to = "0x000000000000000000000000000000000000dead";
		BigInteger nonce = web3j.ethGetTransactionCount(from, DefaultBlockParameterName.LATEST).send()
				.getTransactionCount();
		BigInteger value = BigInteger.valueOf(100);
		String data = "Klaytn Web3j";
		byte[] payload = data.getBytes();

		TxType.Type type = Type.FEE_DELEGATED_VALUE_TRANSFER_MEMO;

		KlayRawTransaction raw = KlayRawTransaction.createTransaction(
				type,
				nonce,
				GAS_PRICE,
				GAS_LIMIT,
				to,
				value,
				from,
				payload);

		// Sign as sender
		byte[] signedMessage = KlayTransactionEncoder.signMessage(raw, chainId, credentials);

		// Sign same message as Fee payer
		signedMessage = KlayTransactionEncoder.signMessageAsFeePayer(raw, chainId, credentials_feepayer);

		String hexValue = Numeric.toHexString(signedMessage);
		EthSendTransaction transactionResponse = web3j.ethSendRawTransaction(hexValue).send();
		System.out.println("TxHash : \n " + transactionResponse.getResult());
		String txHash = transactionResponse.getResult();
		try {
			Thread.sleep(2000);
		} catch (Exception e) {
			System.out.println(e);
		}
		TransactionReceipt receipt = web3j.klayGetTransactionReceipt(txHash).send().getResult();
		System.out.println("receipt : \n" + receipt);
		web3j.shutdown();

		TxTypeValueTransfer rawTransaction = TxTypeValueTransfer.decodeFromRawTransaction(hexValue);
		System.out.println("TxType : " + rawTransaction.getKlayType());

	}

}
```




---
```zsh output
❯ node functionName.js
output
```
</CH.Code>






---

Import Web3j and related classes for blockchain interaction.

```java FeeDelegatedValueTransferMemo.java focus=1:19

```
---

Create a Web3j instance connected to the BAOBAB testnet.
```java FeeDelegatedValueTransferMemo.java focus=23

```
---
Load Klaytn credentials from a sender's private key.

```java FeeDelegatedValueTransferMemo.java focus=24

```
---
Load Klaytn credentials from a fee payer's private key.

```java FeeDelegatedValueTransferMemo.java focus=25

```
---
Declare transaction parameters

```java FeeDelegatedValueTransferMemo.java focus=27:40

```
---
Create a raw transaction object.

```java FeeDelegatedValueTransferMemo.java focus=41:49

```
---
Sign the raw transaction as the sender.

```java FeeDelegatedValueTransferMemo.java focus=51:52

```
---
Sign the same message as the fee payer.

```java FeeDelegatedValueTransferMemo.java focus=54:55

```
---
Send the signed transaction to the Klaytn node.

```java FeeDelegatedValueTransferMemo.java focus=57:59

```
---
Retrieve and print the transaction receipt.

```java FeeDelegatedValueTransferMemo.java focus=60:67

```
---
Shutdown the Web3j instance.

```java FeeDelegatedValueTransferMemo.java focus=68

```
---
Decode the raw transaction to get the transaction type.

```java FeeDelegatedValueTransferMemo.java focus=70:71

```

</CH.Spotlight>