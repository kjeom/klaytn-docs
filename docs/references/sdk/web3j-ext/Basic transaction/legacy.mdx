TxTypeLegacyTransaction represents a type of transactions existed previously in Klaytn.

\*\*This type of transaction can create an account, transfer tokens, deploy a smart contract, execute a smart contract, or perform a mix of aforementioned.

<CH.Spotlight>

<CH.Code>

```java txTypeLegacyTransaction.java
package org.web3j.example;
import org.web3j.protocol.klaytn.Web3j;
import java.io.IOException;
import java.math.BigInteger;
import org.web3j.crypto.Credentials;
import org.web3j.crypto.KlayCredentials;
import org.web3j.crypto.KlayRawTransaction;
import org.web3j.crypto.KlayTransactionEncoder;
import org.web3j.crypto.RawTransaction;
import org.web3j.protocol.core.DefaultBlockParameterName;
import org.web3j.protocol.core.methods.response.EthChainId;
import org.web3j.protocol.core.methods.response.EthSendTransaction;
import org.web3j.protocol.http.HttpService;
import org.web3j.utils.Numeric;
import org.web3j.protocol.klaytn.core.method.response.TransactionReceipt;

public class LegacyExample implements keySample {
        public static void run() throws IOException {
                Web3j web3j = Web3j.build(new HttpService("https://public-en-baobab.klaytn.net"));
                KlayCredentials klaycredentials = KlayCredentials.create("0x4a72b3d09c3d5e28e8652e0111f9c4ce252e8299aad95bb219a38eb0a3f4da49");
                Credentials credentials = Credentials.create("0x4a72b3d09c3d5e28e8652e0111f9c4ce252e8299aad95bb219a38eb0a3f4da49");
                BigInteger GAS_PRICE = BigInteger.valueOf(50000000000L);
                BigInteger GAS_LIMIT = BigInteger.valueOf(6721950);
                String from = credentials.getAddress();
                String to = "0x000000000000000000000000000000000000dead";
                EthChainId EthchainId = web3j.ethChainId().send();
                long chainId = EthchainId.getChainId().longValue();
                BigInteger nonce = web3j.ethGetTransactionCount(from, DefaultBlockParameterName.LATEST).send()
                                .getTransactionCount();
                BigInteger value = BigInteger.valueOf(100);
                raw = KlayRawTransaction.createEtherTransaction(
                                nonce,
                                GAS_PRICE,
                                GAS_LIMIT,
                                to,
                                value);
                signedMessage = KlayTransactionEncoder.signMessage(raw, chainId, klaycredentials);
                hexValue = Numeric.toHexString(signedMessage);
                transactionResponse = web3j.ethSendRawTransaction(hexValue).send();
                System.out.println("TxHash : \n " + transactionResponse.getResult());
                String txHash = transactionResponse.getResult();
                try {
                        Thread.sleep(2000);
                } catch (Exception e) {
                        System.out.println(e);
                }
                TransactionReceipt receipt = web3j.klayGetTransactionReceipt(txHash).send().getResult();
                System.out.println("receipt : \n" + receipt);
                web3j.shutdown();
        }
}
```

---

```zsh output
❯ java txTypeLegacyTransaction.java
3.523785565427238379
0xa02e0i90faef4323cb542312de9aac0
```

</CH.Code>

---

Import the `web3j-ext` package and corresponding class that can interact with the Ethereum Blockchain and its ecosystem.

```java txTypeLegacyTransaction.java focus=2:15

```

---

Setup the provider. A provider in ethers is a read-only abstraction to access the blockchain data.

```java txTypeLegacyTransaction.java focus=19

```

---

Create KlayCredentials or Credentials in order to sign transaction.

```java txTypeLegacyTransaction.java focus=20:21

```
---

Define Gas price and Gas limit for transaction.

```java txTypeLegacyTransaction.java focus=22:23

```
---
Determine from, to address.

```java txTypeLegacyTransaction.java focus=24:25

```
---
Get chain id and nonce

```java txTypeLegacyTransaction.java focus=26:29

```
---
Define value of transaction

```java txTypeLegacyTransaction.java focus=30

```
---
Create raw transaction

```java txTypeLegacyTransaction.java focus=31:36

```
---
Sign transaction with with web3j-ext klaycredentials and encode to Hex string.

```java txTypeLegacyTransaction.java focus=37:38

```
---
**ethSendRawTransaction** method send signed transaction with web3j-credentials.

```java txTypeLegacyTransaction.java focus=39:40

```
---

Get the receipt hash.

<CH.Code>

```java txTypeLegacyTransaction.java focus=47:48

```
---

```zsh output
❯ java txTypeLegacyTransaction.java
0xa02e0i90faef4323cb542312de9aac0
```

</CH.Code>
---
Finally, Shut down web3j instance

```java txTypeLegacyTransaction.java focus=49
```

</CH.Spotlight>
